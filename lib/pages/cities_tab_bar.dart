import 'package:flutter/material.dart';

class CitiesTabBar extends StatelessWidget {
  const CitiesTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Cities Content'),);
  }
}
