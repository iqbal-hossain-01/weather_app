import 'package:flutter/material.dart';

class CurrentTabBar extends StatelessWidget {
  const CurrentTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Current Data"),);
  }
}
